import { InputRestrictDirective } from './input-restrict.directive';

describe('InputRestrictDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRestrictDirective();
    expect(directive).toBeTruthy();
  });
});
